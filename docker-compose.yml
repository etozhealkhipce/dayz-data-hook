services:
  database:
    image: postgres:16-alpine
    container_name: dayz-tracker-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: dayz_tracker
      POSTGRES_USER: ${PGUSER:-dayz_admin}
      POSTGRES_PASSWORD: ${PGPASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PGUSER:-dayz_admin}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dayz-network

  migrate:
    build:
      context: .
      target: migrate
    container_name: dayz-tracker-migrate
    command:
      [
        "sh",
        "-c",
        "apk add --no-cache postgresql-client && until pg_isready -h database -p 5432 -U ${PGUSER:-dayz_admin}; do sleep 2; done && npx drizzle-kit push",
      ]
    depends_on:
      database:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${PGUSER:-dayz_admin}:${PGPASSWORD:-postgres}@database:5432/dayz_tracker
      PGHOST: database
      PGPORT: 5432
      PGDATABASE: dayz_tracker
      PGUSER: ${PGUSER:-dayz_admin}
      PGPASSWORD: ${PGPASSWORD:-postgres}
    networks:
      - dayz-network

  backend:
    build:
      context: .
    container_name: dayz-tracker-backend
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
    environment:
      NODE_ENV: production
      PORT: 5000
      DATABASE_URL: postgresql://${PGUSER:-dayz_admin}:${PGPASSWORD:-postgres}@database:5432/dayz_tracker
      PGHOST: database
      PGPORT: 5432
      PGDATABASE: dayz_tracker
      PGUSER: ${PGUSER:-dayz_admin}
      PGPASSWORD: ${PGPASSWORD:-postgres}
      SESSION_SECRET: ${SESSION_SECRET:-dev-secret-change-in-production}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-}
    ports:
      - "80:5000" # Backend + Frontend на порту 80
    networks:
      - dayz-network
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:5000/health",
        ]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

volumes:
  postgres_data:
    driver: local

networks:
  dayz-network:
    driver: bridge
